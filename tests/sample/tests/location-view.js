define(["mobileui/ui/navigator-card-view"], function(NavigatorCardView) {

    var LocationView = NavigatorCardView.extend({

    });

    return {
        label: "Location View",
        view: LocationView
    };

});